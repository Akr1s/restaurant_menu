import baseUrl from './baseUrl';

const infoServiceRoutes = {
  getRestInfo: `${baseUrl}info/rest`,
  getTitle: `${baseUrl}info/title`,
  getInfo: `${baseUrl}info`,
};

export default infoServiceRoutes;
